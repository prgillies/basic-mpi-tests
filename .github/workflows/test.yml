name: test

on:
  push:
    branches:
      - master
  pull_request:
    branches:
      - master

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@master
    - name: Compile
      run: gfortran hello.f90 -o hello.exe
    - name: Run test
      run: ./hello.exe
    
